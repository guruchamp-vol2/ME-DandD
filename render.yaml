services:
  - type: web
    name: dnd-lobbies-pro
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
      # Optional: add for persistence
      # - key: MONGODB_URI
      #   sync: false
      # - key: MONGODB_DB
      #   value: dnd
